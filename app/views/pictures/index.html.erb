<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

<link href="../../dist/css/bootstrap.min.css" rel="stylesheet">
<div class="jumbotron">
<h1>Listing images</h1>
<a class="btn btn-success" href="/pictures/new" role="button">New picture</a>
</div>
<div class="row">
<div class="col-md-6">
<table class="table table-bordered"">
 <thead> 
  <tr>
     <th>Image_Thumbnails</th>
     <th>Name</th>
     <th>Description</th>
     <th>Place</th>
  </tr>
 </thead> 
<tbody>
 <% @pictures.each do |picture| %>
  <tr class="info">
   <td><%= image_tag(picture.image_url, :alt => "Image", :class=>"main") %></td> 
   <td><%=picture.name%></td> 
   <td><%=picture.description%></td>
   <td><%=picture.place%></td>
   <td><%=link_to 'Show', picture_path(picture), :class=>"btn btn-info"%></td>
   <td><%=link_to 'Edit', edit_picture_path(picture), :class=>"btn btn-primary"%></td>
   <td><%=link_to 'Destroy', picture_path(picture), :class=>"btn btn-danger",
              method: :delete,
              data: { confirm: 'Are you sure?' }%></td>
   </tr>
 <%end%>
</tbody>
</table>
</div>
</div>
<%= stylesheet_link_tag "pictures.scss" %>
<script src="../../dist/js/bootstrap.min.js"></script>
</html> 
